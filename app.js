const classes=[{id:'oct-17',date:'Friday, October 17',title:'Pet First Aid & CPR',time:'10:00 AM – 2:00 PM',place:'Community Learning Center',seats:12,price:125},{id:'nov-8',date:'Saturday, November 8',title:'Pet First Aid & CPR',time:'9:30 AM – 1:30 PM',place:'Northside Community Hall',seats:16,price:125},{id:'dec-6',date:'Saturday, December 6',title:'Pet First Aid Essentials',time:'10:00 AM – 1:00 PM',place:'Community Learning Center',seats:14,price:125}];let activeClass;const registrations=()=>JSON.parse(localStorage.getItem('ppaRegistrations')||'[]');const saveRegistrations=items=>localStorage.setItem('ppaRegistrations',JSON.stringify(items));const escapeHtml=value=>String(value).replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));document.getElementById('class-list').innerHTML=classes.map(course=>`<article class="class-card"><p class="date-label">${course.date}</p><h3>${course.title}</h3><p class="class-meta">${course.time}<br>${course.place}<br>${course.seats} seats available</p><p><strong>$${course.price}</strong> per student</p><button class="button register-button" data-class="${course.id}">Select this class</button></article>`).join('');document.getElementById('year').textContent=new Date().getFullYear();const modal=document.getElementById('registration');function closeRegistration(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true')}function showForm(){document.getElementById('registration-content').innerHTML=`<p class="eyebrow">Reserve your place</p><h2 id="registration-title">${activeClass.title}</h2><p>${activeClass.date} · ${activeClass.time}<br>${activeClass.place} · $${activeClass.price} per student</p><form id="registration-form"><div class="form-grid"><label>Email*<input name="email" type="email" required></label><label>First name*<input name="firstName" required></label><label>Last name*<input name="lastName" required></label><label>Language<select name="language"><option value="english">English</option><option value="spanish">Spanish</option><option value="french">French</option><option value="other">Other</option></select></label></div><fieldset><legend>Would you like to add a pet first aid kit?</legend><label class="radio-option"><input type="radio" name="kit" value="no" checked><span><strong>Class only</strong>Course registration · $${activeClass.price}</span></label><label class="radio-option"><input type="radio" name="kit" value="yes"><span><strong>Add a first aid kit</strong>Course + kit · $${activeClass.price+25}</span></label></fieldset><button class="button" type="submit">Continue to demo checkout</button><p class="form-note">This is a demo checkout. Payments and registration storage will be connected securely before launch.</p></form>`;document.getElementById('registration-form').addEventListener('submit',submitRegistration)}function openRegistration(id){activeClass=classes.find(course=>course.id===id);modal.classList.add('open');modal.setAttribute('aria-hidden','false');showForm()}function submitRegistration(event){event.preventDefault();const data=Object.fromEntries(new FormData(event.target));const all=registrations();all.push({...data,classTitle:activeClass.title,classDate:activeClass.date});saveRegistrations(all);document.getElementById('registration-content').innerHTML=`<div class="confirmation"><div class="success">✓</div><p class="eyebrow">Demo checkout complete</p><h2>You’re on the list!</h2><p>Thanks, ${escapeHtml(data.firstName)}. In the live site, Stripe will securely take payment and send an email confirmation here.</p><button class="button" data-close>Done</button></div>`}function showKitOnlyForm(){modal.classList.add('open');modal.setAttribute('aria-hidden','false');document.getElementById('registration-content').innerHTML=`<p class="eyebrow">Pet first aid kit</p><h2>Be ready at home and on the go.</h2><p>Order the Adventure First Aid Kit on its own for <strong>$40</strong>.</p><form id="kit-only-form"><div class="form-grid"><label class="full">Email*<input name="email" type="email" required></label><label>First name*<input name="firstName" required></label><label>Last name*<input name="lastName" required></label></div><button class="button" type="submit">Continue to demo checkout · $40</button><p class="form-note">This is a demo checkout. Secure payment and shipping details will be connected before launch.</p></form>`;document.getElementById('kit-only-form').addEventListener('submit',event=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.target));document.getElementById('registration-content').innerHTML=`<div class="confirmation"><div class="success">✓</div><p class="eyebrow">Demo checkout complete</p><h2>Your kit is reserved!</h2><p>Thanks, ${escapeHtml(data.firstName)}. In the live site, Stripe will securely take payment and collect your shipping details.</p><button class="button" data-close>Done</button></div>`})}document.addEventListener('click',event=>{const button=event.target.closest('.register-button');if(button)openRegistration(button.dataset.class);if(event.target.closest('[data-close]'))closeRegistration()});document.addEventListener('keydown',event=>{if(event.key==='Escape')closeRegistration()});const kitPrice=document.querySelector('.kit-price'),kitLink=document.querySelector('.kit-card .text-link');kitPrice.innerHTML='$40 <small>kit only</small>';kitLink.innerHTML='Buy a kit <span>→</span>';kitLink.addEventListener('click',event=>{event.preventDefault();showKitOnlyForm()});const menuButton=document.querySelector('.menu-button'),nav=document.querySelector('.site-nav');menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',open)});
